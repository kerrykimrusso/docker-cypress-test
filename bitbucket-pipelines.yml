pipelines:
  default:
    - step:
        caches:
          - node
        script: 
          - docker build -t ui .
          - docker run -p 3000:3000 -p 3001:3001 ui
          - npm build --production
          - $(npm bin)/cypress run
        services:
          - docker
        